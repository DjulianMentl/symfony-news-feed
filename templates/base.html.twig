<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header>
            <div class="logo">
                <h1>Новостная лента</h1>
            </div>

            <nav>
                <ul>
{#                    <li><a href="{{ path('/') }}">Главная</a></li>#}
                    <li><a href="{{ path('show_all_news') }}">Новости</a></li>

                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li><a href="{{ path('logout') }}">Выйти</a></li>
                    {% else %}
                        <li><a href="{{ path('login') }}">Войти</a></li>
                    {% endif %}
                </ul>
            </nav>

            {% if latestNews is defined %}
                <div class="latest-news">
                    Дата последней новости: {{ latestNews.publishedAt|date("d.m.Y") }}
                </div>
            {% endif %}
        </header>

        {% block body %}{% endblock %}

        <footer>
            &copy; {{ "now"|date("Y") }} News Feed. Все права защищены.
        </footer>
    </body>
</html>
